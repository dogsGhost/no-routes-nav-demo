import React from 'react'

// the Contact and Portfolio components are nearly identical in content
// but are written using alternate syntax
var Home = React.createClass({
  render: function () {
    return (
      <p>Homepage in the house!</p>
    )
  }
})

export default Home
